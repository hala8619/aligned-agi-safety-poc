# Core dependencies (必須依存)
numpy>=1.26.0

# Testing (テスト用)
pytest>=7.0.0

# Optional dependencies for advanced features (オプション依存 - 高度な機能用)
# Uncomment the following for BERT-based semantic similarity and LLM integration:
# transformers>=4.30.0
# torch>=2.0.0
# sentencepiece>=0.1.99  # For multilingual models

# Note: v5 hierarchical threshold system achieves 91.1% Recall using
# pattern matching only (no BERT required for core functionality)